{
	"$schema": "https://turbo.build/schema.json",
	"globalDependencies": [".env.development"],
	"pipeline": {
		"build": {
			"dependsOn": ["^build"],
			"outputs": ["dist/**", "src/generated/**"]
		},
		"lint": {},
		"dev": {
			"cache": false,
			"persistent": false
		}
	}
}
