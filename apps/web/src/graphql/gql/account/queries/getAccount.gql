query getAccount($userId: String) {
	getAccount(userId: $userId) {
		_updatedAt
		_createdAt
		userId
		myId
		username
		staus
		emailVerification
		phoneVerification
		profilePictureURL
		prefs {
			location
			termsAccepted
		}
	}
}
