query getListOfExperiencesWithCreator($input: ExperienceInput) {
	getListOfExperiences(input: $input) {
		_createdAt
		_updatedAt
		_collectionId
		_id
		_permissions
		_databaseId
		userId
		imgSrc
		location
		user {
			userId
			myId
			username
			profilePictureURL
		}
	}
}
