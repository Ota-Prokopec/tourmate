import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
	appId: 'com.example.app',
	appName: 'experiences',
	webDir: '.svelte-kit/output',
	bundledWebRuntime: false
};

export default config;
