{
	"name": "node-test",
	"version": "0.0.1",
	"main": "./src/index.ts",
	"license": "MIT",
	"private": true,
	"scripts": {
		"dev": "console-ninja nodemon -w *.ts -e ts -x ts-node --files -H -T src/index.ts",
		"build": "tsc -build",
		"tsup": "tsup src/**"
	},
	"dependencies": {
		"@app/test": "*",
		"express": "^4.18.2",
		"tsup": "^8.0.1"
	}
}
